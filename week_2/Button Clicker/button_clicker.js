// Change login button to say logout when clicked
function logout(element){
    element.innerText = "Logout";
}

// Remove the Add Definition button when it is clicked
function removeAddDef(element){
    element.remove();
}
